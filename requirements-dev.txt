# Development and test dependencies. Install with:
#   pip install -r requirements-dev.txt
# (includes runtime deps via the -r line below)
-r requirements.txt
pytest==8.4.1
pytest-cov==6.2.1
pytest-asyncio==1.0.0
ruff==0.12.2
mypy==1.16.1
